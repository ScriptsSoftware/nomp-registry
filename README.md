nompjs

A simple Node.js package manager.

Install

npm install -g nompjs
Commands

lnstall a package:

nomp install <package>

Example:

nomp install express

Uninstall a package:

nomp uninstall <package>
